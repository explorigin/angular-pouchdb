// Generated by CoffeeScript 1.6.3
describe('angular-pouchdb', function() {
  beforeEach(module('pouchdb'));
  return it("should be able to create a pouchdb database", inject(function(pouchdb, $rootScope) {
    var db, docId, orig, worker;
    docId = '' + Math.random();
    worker = pouchdb.getWorker('../angular-pouchdb-worker.js', '/base/external/pouchdb-nightly.js');
    db = pouchdb.create(docId);
    orig = new PouchDB(docId);
    if (worker === false) {
      return expect(db.type()).toBe(orig.type());
    } else {
      runs(function() {});
      waitsFor(function() {
        return db.type() !== null;
      });
      return runs(function() {
        return expect(db.type()).toBe(orig.type());
      });
    }
  }));
});
